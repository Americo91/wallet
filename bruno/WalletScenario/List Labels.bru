meta {
  name: List Labels
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/labels?name=Fixed Cost
  body: none
  auth: none
}

params:query {
  name: Fixed Cost
}

script:post-response {
  var label = res.body[0].id
  bru.setEnvVar("labelId", label)
}
