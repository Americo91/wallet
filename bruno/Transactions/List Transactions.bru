meta {
  name: List Transactions
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/transactions
  body: none
  auth: none
}

script:post-response {
  var transactionId = res.body[0].id
  bru.setEnvVar("transactionId", transactionId)
}
